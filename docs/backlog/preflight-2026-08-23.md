# Pre-flight — 2026-08-23

Generated by the `er-backlog-preflight` skill from `docs/DesignDoc.md` and `docs/DbtBestPractices.md`.
Nothing here closes a decision. Recommendations are quoted and attributed; per §B.5 item 6, every CONFLICT, OPEN and MISSING row is still yours to close.

## Verdict

**No stage can be planned to completion today, because the stage inventory itself is in conflict.** `DesignDoc.md` §B.3 carries **DR-11 as CONFLICT** — §5 and A.5 are two normative inventories, and §5 is the one a builder plans from while being the incomplete one. Five further rows (DR-16, DR-17 MISSING; B.1, B.8, DR-12 open with fired deadlines) block Stages 0.3, 1, 5 and 6b specifically.

Three tracks of real, unblocked work exist right now, and one of them — rebuilding the deleted engineering scaffold — appears in **no task list in either document**.

## Blocked on a decision

| Blocker | Class | Blocks | Doc's recommendation |
|---|---|---|---|
| **DR-11** Stage inventory | **A — CONFLICT** | Every stage. §5 omits Stages 2b/6b/12b, sub-stages 0.6/0.7, three extensions to 0.3, the critical-path statement, and ~12 per-stage extensions | R3: *"Reconcile to one list."* Scope is enumerated by R3 and extended by RC29 |
| **DR-17** Model JSON trust boundary | **B — MISSING** | Stage 1 | None stated. G3: the model JSON is untrusted SQL executed with the consumer's credentials. RC8 calls this "the one that is architecture" |
| **DR-16** Input contract | **B — MISSING** | Stage 1; any model reading `stg_input` | None stated (G2, G9). G9: the most likely input precondition fails silently |
| **B.1** Runtime substrate | **C — deadline fired** | Stage 0.3 (the harness) | *"(a)"* — dbt writes to `:memory:`, models export to parquet, harness reads only parquet. RC45: the Appendix C `profiles.yml` is already option-(b)-shaped, so scaffolding rebuilds it verbatim and B.1 resolves itself by drift |
| **B.8** ST05 under the CTE ban | **C — fired, and has no register row** | Stages 1 and 5 | *"(a), after testing (c)."* §11.1 concedes `er_int_scored_pairs` cannot satisfy both rules at once until this closes. RC46: needs a DR entry |
| **DR-12** `entity_id` vs `component_label` | **C — trigger fired** | Stage 6b, and `entity_id` everywhere in Stage 6 | Row says "decide with DR-14"; DR-14 has been CURRENT since 2026-08-20, so the trigger fired without the row closing (RC16) |
| **DR-09** One threshold or a gray band | OPEN, marked *blocks Stage 6* | Stage 6 | None stated (M12 rec 5, G15) |
| **DR-08** / **B.2** Threshold as var or dimension | OPEN | Stage 6 — changes the *contract* of `er_int_edges` and `er_entity_clusters`, not just their SQL | *"The dimension."* Stage 6's AC needs three thresholds simultaneously, and cross-threshold monotonicity is not expressible as a dbt test under the var approach |

Also open but scheduled, not blocking: DR-05 (clustering performance path — D4b after Stage 6), B.3 (DuckDB pin — stay on 1.5.5), B.5/B.6 (perf gate calibration and history), B.7 (observability redaction).

## Startable now

| Work | Why it is unblocked | Size | Parallel with |
|---|---|---|---|
| **Rebuild the engineering scaffold** from `DbtBestPractices.md` Appendix C, with the v2 delta tables applied | Appendix C is fully specified and copy-pasteable. Appendix D records that the verified original was deleted, so Appendix C is currently the only copy | days | everything |
| **Comparator sensitivity suite** (A.5 Stage 0.7) | §12.7 gives the mutant catalogue verbatim — 8 mutants, no survivors, each asserting the expected localisation string. M10 sizes it at one day and calls it *"the cheapest credibility available"* | ~1 day | everything |
| **The decision tickets themselves** (DR-11, DR-17, DR-16, B.1, B.8, DR-12) | Deliverable is a doc edit plus a register row | hours each | everything |
| **B.8 option (c) spike** — `EXPLAIN ANALYZE` on a DuckDB lateral column alias: does it evaluate once? | RC46: without it, option (a) is adopted untested by default. Stage 0 is where the plan puts spikes | days | everything |
| **Stage 0.2** — vendor `fake_1000`, seeded synthetic generator | No blocking decision touches it | days | everything |

Two sequencing facts inside that list: the comparator suite is built **before** Stage 0.4 freezes the baselines it will compare (a comparator mutation-tested afterwards has earlier green results nobody can trust retrospectively), and the scaffold rebuild must apply the v2 deltas rather than restoring Appendix C verbatim — restoring it verbatim is exactly how B.1 gets resolved by drift.

## Recommended order

RC8's proposal, which is a **review-note proposal, not a normative section**, plus two items it predates:

1. **Close DR-11 / R3** — one stage list. Everything sequences off it; G6's inventory and G21's register both key off it.
2. **Decide DR-17 / G3**, then disposition the remaining MISSING rows. Check RC30 first: DR-18, DR-19 and DR-20 are now *partially* answered by the companion, so only DR-16 and DR-17 are missing in full.
3. **Settle B.1** — its own text says it must precede the Stage 0.3 harness.
4. **Rebuild the scaffold** from Appendix C with the v2 deltas.
5. **Build the comparator sensitivity suite** (Stage 0.7).
6. Then Stage 0.1–0.5.

Added: **B.8's option-(c) spike** belongs at step 4–5 (RC46), and **DR-16** gates any model reading `stg_input`, so it wants closing alongside DR-17 at step 2.

## Not yet plannable

- **Stages 1 and 5** — B.8 unresolved; Stage 1 additionally needs DR-16/DR-17.
- **Stage 6** — DR-09 (gray band) and DR-08/B.2 (threshold as var or dimension) both change its contract. RC9 also flags `entity_clusters_1to1` as dead code under Stage 12.1's v1 matrix.
- **Stage 6b** — DR-12 is the stage.
- **Stages 8 and 2b** — RC10/RC15: D11 decided `table` everywhere without carving out `incremental`, while Stage 8 still ships the incremental path. A.5's Stage 2b row names this exact state "not acceptable". Resolve the D11 / Stage 8 / Stage 2b triangle first.
- **Stage 9** — its §5 acceptance criterion is unfalsifiable (B5 measured a 1.63 match-weight spread under Splink's default `seed=None`). A.5's trace oracle replaces it, but the trace must be captured back in Stage 0.3, so this depends on B.1 too.
- **Stage 10** — A.5 moves its measurement models to just after Stage 2 so they can gate Stage 3's recall floor and Stage 6's quality tests. That move is part of DR-11's scope.
- **Everything downstream of Stage 1**, in the weak sense that Stage 1 is the critical path: every downstream *baseline* is meaningless until `load_model_json`'s five recomputed values are right.

## One thing that is not a decision, and is worth deciding anyway

§A.6 Q5 has no register row and no owner: **what is the quality floor?** The frozen reference model in Stage 0.4 measures **F1 = 0.72, blocking recall = 0.51** on `fake_1000`; a two-rule change lifts F1 to 0.98. Parity gates cannot see the difference. Without an owner for the committed per-fixture F1 and recall floors, Stage 10 is a reporting stage and the product ships at 0.72.
