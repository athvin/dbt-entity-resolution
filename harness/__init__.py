"""The pytest parity harness (`DesignDoc` Stage 0).

Comparators written to A.4's corrected tolerance table, the §12.7 mutant
catalogue that proves they can fail, and the known-good outputs both run
against. Never under `tests/`, which is dbt's `test-paths`.
"""
