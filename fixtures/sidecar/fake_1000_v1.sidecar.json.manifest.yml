---
# Provenance for the A.2 COMPILE-TIME SIDECAR (3.62).
#
# Generated by scripts/er_sidecar.py, never by hand. A.2 requires a
# byte-equality regeneration test, and `--check` is it: a generated file that
# has drifted from its generator is worse than no generated file, because every
# downstream consumer trusts it.
#
# This artefact resolves the three things A.2 says Jinja provably cannot:
# C2 TF exact-match levels (sqlglot CNF analysis, not string matching),
# C3 the backend link_type and whether a source dataset really exists,
# C4 the two-table l/r orientation (null here -- it is not in the JSON).
kind: baseline
# 3.55: derived from a synthetic fixture and contains no person data at all --
# only resolved probabilities and structural facts.
synthetic: true
splink_version: "4.0.16"
model_json_sha256: "02ede04136994e937fa685b0e8beaf1b7d97ca4a8ecb8f2bd0631abcd8e21c44"
seed: 20260823
duckdb_version: "1.5.5"
sqlglot_version: "30.17.0"
platform:
  os: "darwin"
  architecture: "arm64"
  duckdb_build: "1.5.5"
date: "2026-08-23"
producing_commit: "7b77399"
regenerate_with: >-
  uv run python scripts/er_sidecar.py fixtures/model_jsons/fake_1000_v1.json
  --out fixtures/sidecar/fake_1000_v1.sidecar.json
